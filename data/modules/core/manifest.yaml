name: Core
desc: Core module
version: 1.0.0

deps:
    - engine

ui-docs:
  menu:
    path: menu.rml
  load-game:
    path: load-game.rml
  options:
    path: options.rml
  mod-options:
    path: mod-option-list.rml
  display:
    path: display.rml
  graphics:
    path: graphics.rml
  audio:
    path: audio.rml
  tests:
    path: tests.rml
  connect-to-host:
    path: connect-to-host.rml
  chat:
    path: chat.rml
  debug:
    path: debug.rml