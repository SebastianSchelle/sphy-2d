#ifndef SPHY_BINDINGS_HPP
#define SPHY_BINDINGS_HPP

namespace mod
{

// Force linker to keep the translation unit that registers the daScript module.
void touchSphyBindingsModule();

}  // namespace mod

#endif
